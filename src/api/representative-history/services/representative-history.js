'use strict';

/**
 * representative-history service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::representative-history.representative-history');
