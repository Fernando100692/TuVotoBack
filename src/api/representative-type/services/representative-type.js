'use strict';

/**
 * representative-type service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::representative-type.representative-type');
